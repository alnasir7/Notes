import React from "react";

const NetworkDown = () => {
  return (
    <div
      className="container"
      style={{ position: "relative", top: 50, left: 0 }}
    >
      <h1>Network is down currently, return in a few minutes</h1>
    </div>
  );
};

export default NetworkDown;
